<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="se.grp3.theorganizer">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".TodoActivity"></activity>
        <activity android:name=".MonitorCollabActivity" />
        <activity android:name=".CreateCollabActivity" />
        <activity android:name=".CollabActivity" />
        <activity android:name=".RecommenderActivity" />
        <activity android:name=".TimerActivity" />
        <activity android:name=".PlannerActivity" />
        <activity android:name=".ReminderActivity" />
        <activity android:name=".InputActivity" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>